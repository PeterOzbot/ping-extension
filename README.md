# ping-extension

Gnome extension used to display current ping in toolbar.

### Deploy
Copy to /home/$USER/.local/share/gnome-shell/extensions/ping@val-tech
alt+f2 => command: r

### Logs:
sudo journalctl /usr/bin/gnome-shell | grep -B 5 -A 5 ping@val-tech

